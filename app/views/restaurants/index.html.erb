<div class="container">
  <div class="content">
    <h1>Restaurants Listing</h1>
      <% @restaurants.each do |restaurant| %>
        <h1><%= restaurant.name %></h1>
        <h2><%= restaurant.category %></h2>
        <%= link_to 'show restaurant details', restaurant_path(restaurant), class: "btn btn-primary" %>
        <br>
      <% end %>

<h3><%= link_to 'create the restaurant', new_restaurant_path %></h3>
</div>
</div>
